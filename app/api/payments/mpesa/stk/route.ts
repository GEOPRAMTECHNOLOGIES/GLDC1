import { NextResponse } from "next/server"; import { currentUser } from "../../../../../lib/auth"; import { getDb } from "../../../../../lib/db"; import { stkPush } from "../../../../../lib/mpesa"; import { ObjectId } from "mongodb";
export async function POST(req:Request){try{const me=await currentUser();if(!me)return NextResponse.json({error:'Unauthorized'},{status:401});const {leadId,amount,phone,description}=await req.json();const db=await getDb();const lead=await db.collection('leads').findOne({_id:new ObjectId(leadId),memberId:new ObjectId(me.id),status:'approved',paymentStatus:{$in:[null,'unpaid']}});if(!lead)return NextResponse.json({error:'Lead must be approved by GLDC before payment.'},{status:403});const payment={leadId:lead._id,memberId:new ObjectId(me.id),amount:Number(amount),phone,description,status:'initiated',createdAt:new Date(),updatedAt:new Date()};const ins=await db.collection('payments').insertOne(payment);try{const mp=await stkPush(phone,Number(amount),`GLDC-${String(ins.insertedId)}`,description||'GLDC project deposit');await db.collection('payments').updateOne({_id:ins.insertedId},{$set:{status:'stk_sent',merchantRequestId:mp.MerchantRequestID,checkoutRequestId:mp.CheckoutRequestID,response:mp,updatedAt:new Date()}});return NextResponse.json({ok:true,paymentId:String(ins.insertedId),mpesa:mp})}catch(err:any){await db.collection('payments').updateOne({_id:ins.insertedId},{$set:{status:'failed',error:err.message,updatedAt:new Date()}});throw err}}catch(e:any){return NextResponse.json({error:e.message||'Payment failed'},{status:400})}}
