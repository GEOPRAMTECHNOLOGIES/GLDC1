'use client'; import {useState} from 'react';
export default function Login(){const [msg,setMsg]=useState('');async function submit(e:any){e.preventDefault();const b=Object.fromEntries(new FormData(e.currentTarget));const r=await fetch('/api/auth/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(b)});const j=await r.json();if(r.ok)location.href=j.user.role==='admin'?'/admin':'/member';else setMsg(j.error||'Login failed')}return <main className="shell"><section className="card narrow"><img src="/gldc-logo.png"/><h1>Sign in</h1><form onSubmit={submit}><label>Email<input name="email" type="email" required/></label><label>Password<input name="password" type="password" required/></label><button className="btn">SIGN IN</button></form>{msg&&<p className="error">{msg}</p>}<a href="/register">Create member account</a></section></main>}
