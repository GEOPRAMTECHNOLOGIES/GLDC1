'use client'; import {useEffect,useState} from 'react';
export default function Member(){const [u,setU]=useState<any>();useEffect(()=>{fetch('/api/auth/me').then(r=>r.json()).then(j=>setU(j.user))},[]);return <main className="shell"><header><img src="/gldc-logo.png"/><a href="/">Home</a></header><section className="card"><h1>Member Account</h1>{u?<><h2>{u.firstName} {u.lastName}</h2><p>{u.email} · {u.status}</p><p>Membership: {u.memberType}</p><p>Create leads only after approval. Payments are enabled only after the related lead has been approved by GLDC administration.</p></>:<p>Loading…</p>}</section></main>}
